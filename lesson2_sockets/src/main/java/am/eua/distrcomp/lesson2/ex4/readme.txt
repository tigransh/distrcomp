Add a History Feature.
Currently, the server processes calculations but does not store previous calculations.
Modify the server so that it keeps track of all calculations and allows the client
to retrieve them by sending "HISTORY".